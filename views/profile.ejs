<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>'s Profile
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="<%=link1%>">
    <link rel="stylesheet" href="<%=link2%>">
    <link rel="canonical" href="https://getbootstrap.com/docs/5.3/examples/masonry/">
    <link rel="stylesheet" href="/css/output.css">
</head>

<body class="bg-gray-900">
    <div class="dashboard-container">
        <nav class="bg-gray-800 shadow-lg">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between h-16">
                    <div class="flex">
                        <a href="/dashboard" class="flex-shrink-0 flex items-center">
                            <i class="text-gray-50 text-xl fa-solid fa-objects-column"></i>
                            <span class="text-gray-300 text-lg ml-2 font-semibold">ProfileHills</span>
                        </a>
                    </div>
                    <form action="/search" class="flex items-center" style="width: 40%;">
                        <input class="h-9 bg-slate-700 rounded-md my-auto w-full outline-none pl-4 text-slate-300"
                            type="search" name="query" id="searchInput" placeholder="Type here to search..." required autocomplete="off">
                        <button id="searchButton"
                            class="px-4 h-9 py-2 bg-slate-900 rounded-md text-slate-400 text-center">search</button>
                    </form>
                    <div class="flex items-center">
                        <div class="hidden md:block">
                            <a href="/following-blogs"
                                class="text-gray-300 hover:bg-gray-700 px-3 py-2 rounded-md text-sm font-medium">Home</a>
                            <a href="#"
                                class="text-gray-300 hover:bg-gray-700 px-3 py-2 rounded-md text-sm font-medium">About</a>
                            <a href="#"
                                class="text-gray-300 hover:bg-gray-700 px-3 py-2 rounded-md text-sm font-medium">Services</a>
                            <a href="/dashboard"
                                class="bg-gray-700 text-gray-300 hover:bg-gray-700 px-3 py-2 rounded-md text-sm font-medium">Dashboard</a>
                        </div>
                    </div>
                    <div class="flex md:hidden">
                        <button class="text-gray-300 hover:bg-gray-700 p-2 rounded-md">
                            <svg class="h-6 w-6" fill="none" stroke-linecap="round" stroke-linejoin="round"
                                stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                                <path d="M4 6h16M4 12h16M4 18h16"></path>
                            </svg>
                        </button>
                    </div>
                </div>
            </div>
        </nav>

        <div class="upperSection h-72 grid grid-cols-3 gap-3" style="border-bottom: 1px solid #1f2630;">
            <div class="png flex flex-col items-center justify-center gap-4">
                <i class="fa-duotone fa-user text-slate-400 text-8xl"></i>
                <p class="text-slate-500" style="width: 80%; text-align: center;">
                    <%=user.college%>
                </p>
            </div>
            <div class="bio gap-3" style="display: flex; justify-content: center; flex-direction: column;">
                <h2 class="text-slate-300 text-xl">
                    <%=user.firstName%>
                        <%=user.lastName%> <span class="text-sm">
                                <%=user.country%>
                            </span>
                </h2>
                <h2 class="text-slate-300 text-md">@<%=user.username%>

                        <%if(verified==true) {%>
                            <i class="text-blue-500 fa-solid fa-badge-check" style="cursor: pointer;"
                                title="Developer | Verified"></i>
                            <% } %>



                </h2>
                <div class="funTab" style="display: flex; justify-content: space-between;">
                    <div class="followers flex gap-2 flex-col">
                        <h1 class="text-slate-500 font-bold">Followers</h1>
                        <h1 class="text-slate-300 text-xl">
                            <%=followersCnt%>
                        </h1>
                    </div>
                    <div class="following flex gap-2 flex-col">
                        <h1 class="text-slate-500 font-bold">Followings</h1>
                        <h1></h1>
                        <h1 class="text-slate-300 text-xl">
                            <%=followingsCnt%>
                        </h1>
                    </div>
                    <div class="posts flex gap-2 flex-col">
                        <h1 class="text-slate-500 font-bold">Posts</h1>
                        <h1></h1>
                        <h1 class="text-slate-300 text-xl">
                            <%=userBlogs.length%>
                        </h1>
                    </div>
                </div>
                <div class="bioAdding text-slate-300">
                    <p>
                        <%=user.bio%>
                    </p>
                </div>
            </div>
            <div class="settings" style="display: flex; justify-content: center; align-items: center;">
                <% if (user._id.toString() !==rootUser._id.toString()) { %>
                    <% if (rootUser.followings.includes(user._id.toString())) { %>
                        <!-- Show Unfollow button if already following -->
                        <form action="/unfollow/<%= user._id %>" method="post">
                            <button class="bg-red-600 hover:bg-red-800 text-white px-4 py-2 rounded-md"
                                style="transition: 0.4s ease">Unfollow</button>
                        </form>
                        <% } else { %>
                            <!-- Show Follow button if not following -->
                            <form action="/follow/<%= user._id %>" method="post">
                                <button class="bg-blue-600 hover:bg-blue-800 text-white px-4 py-2 ml-3 rounded-md"
                                    style="transition: 0.4s ease">Follow</button>
                            </form>
                            <% } %>
                                <% } else {%>
                                    <div class="me"
                                        style="display: flex; flex-direction: column; gap: 1rem; align-items: center;">
                                        <p class="text-slate-400">This is public view of your profile</p>
                                        <a href="/dashboard" class="text-slate-300 underline">dashboard</a>
                                    </div>
                                    <% } %>
            </div>
        </div>


        <div class="blogPosts mt-8 w-full px-4 mx-auto grid grid-cols-3 gap-4">
            <% userBlogs.forEach(blog=> { %>
                <div class="blogPost bg-slate-700 rounded-md mb-6 p-4">
                    <h2 class="text-slate-200 text-xl font-bold">
                        <%= blog.title %>
                    </h2>
                    <p class="text-slate-300 mt-2">
                        <%= blog.content %>
                    </p>
                    <div class="interactionSection grid mt-4">
                        <div class="containedPack flex justify-between">
                            <form action="/like/<%= blog._id %>" method="POST">
                                <% if (blog.likes.includes(rootUser._id.toString())) { %>
                                    <!-- Show Unlike button if the user has already liked the blog -->
                                    <button class="text-white rounded-md py-2" type="submit">
                                        <i class="fa-solid fa-thumbs-up"></i> <%= blog.likes.length %>
                                    </button>
                                <% } else { %>
                                    <!-- Show Like button if the user hasn't liked the blog -->
                                    <button class="text-slate-300 rounded-md py-2" type="submit">
                                        <i class="fa-light fa-thumbs-up"></i> <%= blog.likes.length %>
                                    </button>
                                <% } %>
                            </form>
                            <button class="bg-blue-800 text-slate-300 rounded-md px-3 py-2 commentButton"
                                data-blog-id="<%= blog._id %>">
                                Comment
                            </button>
                        </div>
                        <a href="/detailed-view/<%= blog._id %>" class="text-slate-400" style="text-decoration: underline;">View all comments</a>
                    </div>
                    
                </div>
            <% }); %>
        </div>
        
        <div id="commentModal" class="modal">
            <div class="modal-content bg-slate-800 rounded-md p-4">
                <span class="close text-white cursor-pointer">&times;</span>
                <form id="commentFormModal" method="POST" class="commentForm mt-4">
                    <input type="text" name="comment" class="w-full h-10 bg-slate-700 rounded-md px-3 py-2 text-slate-300"
                        placeholder="Write a comment..." autocomplete="off" required>
                    <!-- Add an input field for the blog post ID -->
                    <input type="hidden" name="blogId" id="commentBlogId">
                    <button class="bg-blue-800 text-slate-300 rounded-md px-3 py-2 mt-2" type="submit">Post</button>
                </form>
            </div>
        </div>
        
        
        <script>
            const commentButtons = document.querySelectorAll('.commentButton');
            const commentModal = document.getElementById('commentModal');
            const commentFormModal = document.getElementById('commentFormModal');
            const commentBlogIdInput = document.getElementById('commentBlogId');
            const closeCommentModal = commentModal.querySelector('.close');
        
            commentButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const blogId = button.getAttribute('data-blog-id');
                    commentBlogIdInput.value = blogId;
                    commentFormModal.action = `/comment/${blogId}`;
                    commentModal.style.display = 'block';
                });
            });
        
            closeCommentModal.addEventListener('click', () => {
                commentModal.style.display = 'none';
            });
        
            window.addEventListener('click', (event) => {
                if (event.target === commentModal) {
                    commentModal.style.display = 'none';
                }
            });
        </script>
        
    </div>
</body>
</html>
</body>

</html>